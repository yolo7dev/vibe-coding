export type SidebarCategory = {
  label: string;
  hasMenu?: boolean;
};

export type IconCategory = {
  label: string;
  icon: 'phone' | 'computer' | 'watch' | 'camera' | 'headphones' | 'gamepad';
};
